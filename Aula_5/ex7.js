const numeros = [1, 2, 3, 4];
const pares = numeros.filter(num => num%2===0){
    
}

const quadrados = numeros.map(num => num*num); // Map é uma função que aplica na lista para elevar os numeros ao quadrado
console.log(quadrados);